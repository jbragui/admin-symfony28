{% macro fila(name, value) %}
    <div class="form-group">
        <label class="col-sm-2 control-label">{{ name }}:</label>
        <div class="col-sm-10">
            {{ value|nl2br }}
        </div>
    </div>
{% endmacro %}
